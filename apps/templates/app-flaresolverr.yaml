apiVersion: v1
kind: Namespace
metadata:
  name: flaresolverr
  labels:
    name: flaresolverr
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flaresolverr
  namespace: flaresolverr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flaresolverr
  template:
    metadata:
      labels:
        app: flaresolverr
    spec:
      containers:
        - name: flaresolverr
          image: ghcr.io/flaresolverr/flaresolverr:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8191
          env:
            - name: LOG_LEVEL
              value: info
            - name: TZ
              value: Europe/Amsterdam
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            # Headless Chromium solving Cloudflare challenges is memory- and CPU-hungry;
            # the previous 1Gi limit / 256Mi shm caused OOM restarts and
            # "session not created: chrome not reachable" crashes under load.
            limits:
              cpu: "2"
              memory: 3Gi
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 1Gi
        # Chromium creates and discards a profile directory per request. On the
        # container's own filesystem that churn was the single largest source of
        # disk writes on the node, for data that never needs to survive a restart.
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  namespace: flaresolverr
spec:
  type: ClusterIP
  selector:
    app: flaresolverr
  ports:
    - name: http
      port: 8191
      targetPort: 8191
